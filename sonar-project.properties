sonar.projectKey=three-tier-backend
sonar.projectName=three-tier-backend
sonar.sourceEncoding=UTF-8
sonar.sources=.
# Exclude dependencies, build artifacts, tests and docs
sonar.exclusions=**/node_modules/**,**/coverage/**,**/build/**,**/dist/**,**/*.test.js,**/*.spec.js,**/*.md
# If coverage later added, point to lcov file (adjust path accordingly)
# sonar.javascript.lcov.reportPaths=coverage/lcov.info
